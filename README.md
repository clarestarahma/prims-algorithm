# prim-s-algorithm
A Java-based implementation of Prim's Algorithm for finding the Minimum Spanning Tree (MST). Features $O(E \log V)$ time complexity using a Priority Queue and custom Comparable Node objects.
